# T-050 · «السِّجل» علامةً — حذف ختم alsigil

**واجهة · هوية** · تقدير **٤س** · ملحمة **E-5**

**يعتمد على:** T-048. **يمنع:** T-051، T-055.
**يلمس:** `web/components/brand/Alsigil.tsx`، `web/lib/brand/`، `web/scripts/brand/build-marks.py`،
`web/app/layout.tsx`، `web/app/providers.tsx`، `web/components/Shell.tsx`.

---

## القرار

العلامة تصير **«السِّجل»** نصًّا عربيًّا مع أيقونة `gavel`، وتحتها
«إدارة مكاتب المحاماة» — كما في القالب. ويسقط ختم alsigil نهائيًّا.

هذا يحسم أيضًا تسمية كانت معلّقة (alsigil مقابل LegalOS): **«السِّجل» هو اسم المنتج.**

---

## ما يُحذف

| المسار | ما هو |
|---|---|
| [`web/components/brand/Alsigil.tsx`](../web/components/brand/Alsigil.tsx) | مكوّن العلامة — ١٦ KB |
| [`web/lib/brand/geometry.ts`](../web/lib/brand/geometry.ts) | هندسة الثقب والتراصفات |
| [`web/lib/brand/marks.ts`](../web/lib/brand/marks.ts) | مسارات SVG مولَّدة |
| [`web/scripts/brand/build-marks.py`](../web/scripts/brand/build-marks.py) | مولّد المسارات من Archivo |

**احذفها، لا تعطّلها.** مكوّن علامة لا يستدعيه أحد يتعفّن ويُربك من يقرأ الكود بعد شهر.
تاريخ git يحفظها لمن أرادها.

`build-marks.py` يعتمد على Archivo وHarfBuzz — تحقّق أن حذفه يُسقط تبعيات بناء لم تعد
لازمة (`fonttools`، `uharfbuzz`) وأزلها إن لم يستعملها شيء آخر.

**مواضع الاستعمال الثلاثة:** [`layout.tsx`](../web/app/layout.tsx) ·
[`providers.tsx`](../web/app/providers.tsx) · [`Shell.tsx`](../web/components/Shell.tsx).

---

## ما يحلّ محلّه

علامة نصّية بسيطة: أيقونة `gavel` داخل مربّع بـ`--rs`، بجوارها «السِّجل» بوزن ثقيل، وتحتها
السطر الوصفي بـ`--text3`. تنكمش إلى الأيقونة وحدها حين يُطوى الشريط الجانبي.

لا تُعِد بناء نظام قيود العلامة القديم. ورقة الهوية القديمة كانت تمنع تلوين الثقب ومطّه
وإضافة المؤثرات، وكانت تلك القيود مفروضة في نوع البيانات نفسه. العلامة الجديدة نصّ
وأيقونة — لا شيء فيها يحتاج حراسة من هذا النوع، وبناء حراسة له سيكون كلفة بلا مقابل.

**الأيقونة تأخذ لون العلامة** (`--primary`)، فتتبع `--brand-h` تلقائيًّا وتبقى متّسقة مع
ما يختاره المستخدم في T-054. هذه ميزة القالب: لا شعار ثابت يشذّ عن لوحة متغيّرة.

---

## ما يُراجَع لا يُحذف

اسم `alsigil` مستعمل خارج العلامة، وليس كلّه هوية بصرية:

- `deploy/alsigil-*.service` و`*.timer` — أسماء وحدات systemd تعمل في الإنتاج.
  **لا تُمسّ في هذه التذكرة.** إعادة تسميتها تلمس نشرًا حيًّا وتستحقّ تذكرتها.
- `alsigil.com` — النطاق. خارج نطاق الكود.
- `docs/` و`tickets/` — مراجع تاريخية تبقى.

**النطاق هنا: العلامة البصرية في `web/` فقط.**

---

## معايير القبول

- [ ] الملفات الأربعة أعلاه محذوفة، ولا استيراد معطّل يشير إليها
- [ ] العلامة الجديدة في المواضع الثلاثة، وتتبع `--primary`
- [ ] تنكمش إلى الأيقونة عند طيّ الشريط، ولها `aria-label` صحيح
- [ ] `tsc` و`npm run build` خضراوان — لا استيراد ميت
- [ ] تبعيات بناء العلامة أُزيلت إن لم يستعملها شيء آخر
- [ ] `<title>` وبيانات الصفحة الوصفية تقول «السِّجل»

---

## الفحص البشري

- [ ] الشريط مفتوحًا ومطويًّا، في الأوضاع الأربعة — العلامة مقروءة في كلٍّ منها
- [ ] الشريط داكن (`mixed`) والمحتوى فاتح: العلامة لا تختفي في أرضيّتها
- [ ] صفحة الدخول والتسجيل: العلامة ظاهرة وبحجم معقول
- [ ] تبويب المتصفّح يقول «السِّجل» لا «LegalRAG» ولا «alsigil»
