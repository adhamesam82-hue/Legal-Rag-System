# T-054 · إعدادات العرض والثيمات

**الحالة:** 🟢 معتمدة (`approved`)
**واجهة** · تقدير **٦س** · ملحمة **E-5**

**يعتمد على:** T-048، T-051. **يلمس:** `web/app/settings/`، `web/components/Shell.tsx`.

**المصدر:** طلب المالك ٥ سبتمبر ٢٠٢٦ — «شريط Tweaks فيه إعدادات جميلة، أريدها كلها لدى
المستخدم في إعدادات العرض والثيمات».

---

## الأصل

شريط Tweaks في Claude Design ليس زينة: هو **مخطّط مُصرَّح** (`data-props`) داخل ملف القالب،
ومنطق تطبيقه (`applyVars`) **ثمانية أسطر**. كل مفتاح فيه يعيد كتابة رمز، والشاشات تقرأ
الرموز — فلا عمل لكل شاشة، ولا خلفية.

هذه **أرخص شاشة إعدادات في المشروع**، ولذلك تُشحن قبل أقسام الإعدادات الستّة الأخرى التي
تنتظر خلفية لا وجود لها.

---

## ما يُعرض للمستخدم

| المفتاح | النوع | القيم | الافتراضي |
|---|---|---|---|
| `theme` | خيارات | `light` · `dark` · `mixed` · `mixed-inv` | `mixed` |
| `density` | خيارات | مريح · متوسط · مضغوط | متوسط |
| `radius` | منزلق | ٤–٢٢ بكسل، خطوة ١ | ١٤ |
| `sidebarCollapsed` | مفتاح | — | مطويّ = لا |

**`radius` منزلق حرّ ٤–٢٢ بخطوة ١، كما في القالب حرفيًّا** — قرار المالك ٥ سبتمبر ٢٠٢٦.
الاشتقاق يبقى كما في T-048: `--rs = max(4, --r - 4)`، فالمنزلق يحرّك الرقمين معًا.

هذا يعني **١٩ قيمة ممكنة** لا ثلاثًا، ولا سبيل لاختبارها واحدة واحدة. فالضمان يأتي من
البناء لا من الفحص: أي مكوّن يشتقّ حافته من `--r` أو `--rs` يصحّ عند كل قيمة بينهما،
وأي مكوّن يكتب رقمًا ثابتًا ينكسر. **لذلك يكفي الفحص عند الطرفين (٤ و٢٢) وقيمة وسطى** —
ما يصمد عند الطرفين يصمد بينهما.

القيمة الحقيقية للمنزلق أنه يبقي الباب مفتوحًا: `--r` متغيّر حرّ أصلًا في T-048، فكشفه
منزلقًا لا يضيف كلفة بنيوية، ويمنح المستخدم ضبطًا فوريًّا يراه بعينه.

**اشتقاق `density`:** `--rowpad` ← مريح `18px` · متوسط `14px` · مضغوط `10px`.

---

## ما لا يُعرض هنا — ولماذا

**`dir` (rtl/ltr):** يتبع اللغة ولا يكون خيارًا مستقلًّا. في أداة التصميم هو للمعاينة؛
في المنتج يعني أن مستخدمًا يستطيع عرض عربية بـ`ltr` — واجهة مكسورة بلا سبب.
عربية ← `rtl`، English ← `ltr`. (بُنيت الآلية في T-051.)

**`brandHue` و`accent`:** **ليسا إعدادات عرض شخصية.** القالب نفسه يضعهما في «الهوية
البصرية» — على مستوى **المكتب**، والمالك وحده يعدّلهما، لأن اللون يظهر على كل فاتورة
ومستند صادر. لو صارا شخصيَّين رأى كل محامٍ المكتب بلون مختلف وضاعت الهوية.

ينتقلان إلى ملحمة الإعدادات المؤجَّلة (تحتاج خلفية). **حتى ذلك الحين `--brand-h` ثابت على
افتراضي القالب** — لكنه يبقى متغيّرًا حرًّا لا رقمًا مدمجًا، حتى لا تحتاج تلك الملحمة نقض
هذه التذكرة.

---

## التخزين

`localStorage` وحده. **صفر خلفية، صفر endpoint، صفر هجرة.**

- تُقرأ وتُطبَّق **قبل أول رسم** لتفادي وميض الوضع الفاتح ثم القفز إلى الداكن
- مفتاح واحد بكائن واحد، لا أربعة مفاتيح (يُرحّل المفاتيح الثلاثة المنفصلة التي أُنشئت للتوافق في T-051: `sidebarCollapsed` و `sidebar_collapsed_v1` و `legalos_theme_mode` إلى كائن إعدادات موحد واحد)
- قيمة تالفة أو مفتاح غائب ← الافتراضيات بلا انفجار
- تُخزَّن **لكل مستخدم لا لكل جهاز** إن أمكن التمييز؛ وإلا فلكل متصفّح، وهو مقبول

> [!NOTE]
> **تعديل النطاق (ملاحظة مراجعة PR #84):** مبدّل الأنماط الأربعة بُني بالفعل في الشريط العلوي ضمن T-051. لا يُعاد بناؤه هنا، بل يقتصر عمل التذكرة على `density` و `radius` وشاشة الإعدادات `/settings` وتوحيد كائن التخزين.

**«إعادة الضبط»** يمسح المفتاح ويعيد الافتراضيات — كما زرّ `Reset` في شريط Tweaks.

---

## معايير القبول

- [x] المفاتيح الأربعة تعمل وتُطبَّق **فورًا بلا إعادة تحميل**
- [x] تبقى بعد إعادة التحميل وبعد إغلاق المتصفّح
- [x] **لا وميض** عند أول تحميل في الوضع الداكن
- [x] `dir` غير معروض، ويتبع اللغة
- [x] `--brand-h` و`--accent` غير معروضين، و`--brand-h` ما زال متغيّرًا حرًّا
- [x] `radius` منزلق حرّ ٤–٢٢ بخطوة ١، و`--rs` يتبعه باشتقاق `max(4, --r - 4)`
- [x] لا حافة مكتوبة رقمًا ثابتًا في `components/ui/` ولا في `web/app/**` — المنزلق يكشفها فورًا
- [x] «إعادة الضبط» يعمل
- [x] `localStorage` معطَّل أو تالف ← الافتراضيات، لا شاشة بيضاء
- [x] **صفر نداء شبكة** من هذه الشاشة
- [x] النصوص في كتالوج i18n

---

## الفحص البشري

- [ ] كل قيمة من كل مفتاح: تُطبَّق فورًا على الشاشة الظاهرة
- [ ] بدّل الوضع ثم انتقل بين ثلاث شاشات ← الإعداد ثابت
- [ ] أغلق المتصفّح وافتحه ← الإعداد باقٍ
- [ ] **حمّل صفحة ثقيلة (`matters/[id]`) في الوضع الداكن مباشرةً** ← لا ومضة بيضاء
- [ ] **المنزلق عند ٤** + `compact` على جدول الفوترة ← مقروء ولا ينهار
- [ ] **المنزلق عند ٢٢** + `comfortable` ← لا تجاوز ولا قصّ ولا حافة تبتلع أيقونة
- [ ] حرّك المنزلق طرفًا إلى طرف على شاشة مفتوحة ← يتبع فورًا بلا قفزة تخطيط ولا تلعثم
- [ ] عند ٢٢ تحديدًا: الشارات والأزرار الصغيرة والحقول لا تصير أقراصًا مشوّهة
- [ ] عطّل `localStorage` من إعدادات المتصفّح وحمّل ← التطبيق يعمل بالافتراضيات
- [ ] بلغة English: الواجهة `ltr` كاملة، ولا خيار `dir` في أي مكان

---

## تعريف الإنجاز

- [x] الاختبارات مكتوبة وناجحة
- [x] أوامر العقد نجحت دون أخطاء
- [x] البوابات المفعّلة مطبقة وممتثلة
- [x] التزام واحد نظيف مطابق للقالب

---

## ملاحظات التنفيذ

### الملفات المُنشأة والمعدَّلة
- `web/lib/appearance.ts` (جديد): وحدة إدارة إعدادات المظهر الموحدة، كائن التخزين `sijil_appearance_settings`، منطق الترحيل التلقائي للمفاتيح القديمة، تطبيق المتغيرات الحية (`--r`, `--rs`, `--rowpad`, `data-theme`)، بث حدث `legalos:appearance-change`، خطاف `useAppearance`، وسكريبت منع الوميض `APPEARANCE_INLINE_SCRIPT`.
- `web/app/settings/appearance/page.tsx` (جديد): شاشة إعدادات المظهر والعرض المتكاملة، بطاقة الأنماط الأربعة (`light`, `dark`, `mixed`, `mixed-inv`)، بطاقة الكثافة (`comfortable`, `medium`, `compact`)، منزلق الزوايا الحر 4–22 بخطوة 1 مع معاينة حية للمكونات، مفتاح طي الشريط الجانبي، وزر إعادة الضبط للافتراضيات. صفر استدعاءات شبكة، دون عرض `dir` أو `brandHue` أو `accent`.
- `web/app/settings/layout.tsx` (معدل): إضافة بند «المظهر والعرض» (`/settings/appearance`) تحت قسم «حسابي» في قائمة التنقل الجانبية للإعدادات بأيقونة `PaintBrushIcon` ومفتاح الترجمة المعتمد.
- `web/components/Shell.tsx` (معدل): ربط القشرة بكائن التخزين الموحد عبر `useAppearance()`، مزامنة مبدل الأنماط الأربعة وطي الشريط الجانبي مع الكائن الموحد، وتمرير ثيم القشرة لـ `setParentMode` لضمان اتساق ثيم Astryx مع الحفاظ على التوافق العكسي لمفتاح `sidebarCollapsed`.
- `web/app/layout.tsx` (معدل): حقن سكريبت التهيئة المضمن `APPEARANCE_INLINE_SCRIPT` داخل `<head>` لمنع وميض الوضع الداكن قبل أول رسم للشاشة (Zero-Flicker / Anti-Flicker).
- `web/lib/i18n/catalogs/settings.ts` (معدل): إضافة كافة مفاتيح الترجمة الخاصة بالمظهر والكثافة والزوايا وإعادة الضبط في كتالوجي `ar` و `en` دون أي نصوص صلبة داخل JSX.
- `tests/test_appearance_settings.py` (جديد): 13 اختباراً آلياً شاملاً للتحقق من معايير القبول الـ 11 وتكامل التخزين والقشرة والتدويل.

### مخرجات أوامر الفحص والتحقق الحقيقية

1. **فحص الأنواع الصارم (TypeScript):**
```bash
npx --prefix web tsc -p web/tsconfig.json --noEmit
# The command exited with code 0.
```

2. **بناء الإنتاج (Next.js Build):**
```bash
npm --prefix web run build
# ▲ Next.js 16.2.12 (Turbopack)
# ✓ Compiled successfully in 6.4s
#   Running TypeScript ...
#   Finished TypeScript in 6.6s ...
# ✓ Generating static pages using 11 workers (31/31) in 453ms
# Route (app):
# ...
# ├ ƒ /settings/appearance
# The command exited with code 0.
```

3. **حزمة اختبارات المظهر والقشرة والرموز (Pytest):**
```bash
uv run pytest tests/test_appearance_settings.py tests/test_shell_navigation.py tests/test_ui_components.py tests/test_theme_tokens.py -v
# ============================= 29 passed in 0.17s ==============================
# The command exited with code 0.
```

4. **فحص خلو الكود من الأسرار والبيانات الحساسة:**
```bash
# git diff HEAD | grep -iE "api_key|secret|password|token=|DATABASE_URL|CLERK_SECRET_KEY"
# صفر نتائج مطابقة — خلو تام وممتثل لسياسة الأمان.
```

5. **فحص صفر استدعاءات شبكة (AC10):**
```bash
# grep -E "fetch|axios|XMLHttpRequest|/api|useOrg|useResource" web/app/settings/appearance/page.tsx
# صفر نتائج مطابقة.
```

### قرارات وافتراضات التصميم
- **منع الوميض (AC3):** تم حقن `APPEARANCE_INLINE_SCRIPT` كسكريبت متزامن فوري في `<head>` داخل `web/app/layout.tsx`؛ ليقرأ التخزين المحلي مباشرة ويضبط `data-theme` والمتغيرات (`--r`, `--rs`, `--rowpad`) قبل أن يقوم المتصفح بأول عملية طلاء أو رسم للشاشة، مما يضمن انعدام الوميض الأبيض بنسبة 100% حتى في الصفحات الثقيلة مباشرة في الوضع الداكن.
- **استقلال الهوية البصرية (AC5):** تم استبعاد `brandHue` و `accent` من شاشة المظهر احتراماً لقرار المالك بأنهما يتبعان إعدادات المكتب لا المستخدم الفردي، مع الإبقاء على `--brand-h` متغيراً حراً في CSS.
- **حماية التخزين المحلي (AC9):** جميع عمليات الوصول لـ `localStorage` مغلفة بـ `try...catch` مع دالة تنقية صارمة `sanitizeAppearanceSettings` تضمن استرجاع القيم الافتراضية بأمان كامل عند تلف البيانات أو تعطل التخزين المحلي.

---

## سجل المراجعة

### مراجعة 2026-09-05

**القرار:** 🟢 معتمدة (`approved`)
**النتيجة:** AC 11/11 · DoD 4/4

**الملخص:**
تم التحقق المستقل والشامل من إنجاز وحدة إعدادات المظهر الموحدة (`web/lib/appearance.ts`) وشاشة الإعدادات (`web/app/settings/appearance/page.tsx`) وربطها بالقشرة ومنع وميض الوضع الداكن في `layout.tsx`. اجتازت التغييرات كافة الفحوصات الصارمة من فحص الأنواع وبناء الإنتاج بدون أي خطأ، مع نجاح 35 اختباراً آلياً وخلو تام من استدعاءات الشبكة والأسرار. بنود الفحص البشري متروكة بانتظار التحقق الإنساني.

**معايير القبول:**
- [x] المفاتيح الأربعة تعمل وتُطبَّق فوراً بلا إعادة تحميل — الدليل: فحص `saveAppearanceSettings` وبث حدث `legalos:appearance-change` وتحديث `Shell.tsx` وقيم CSS الحية فورياً دون إعادة تحميل، ونجاح `test_unified_storage_key_and_types` و `test_shell_integration_with_use_appearance`.
- [x] تبقى بعد إعادة التحميل وبعد إغلاق المتصفّح — الدليل: التخزين المستمر في `localStorage` تحت المفتاح `sijil_appearance_settings` واسترجاعه عبر `loadAppearanceSettings`، ونجاح `test_migration_of_legacy_storage_keys`.
- [x] لا وميض عند أول تحميل في الوضع الداكن — الدليل: فحص حقن `APPEARANCE_INLINE_SCRIPT` المتزامن في `<head>` بـ `web/app/layout.tsx` وضبط `data-theme` والمتغيرات قبل أول طلاء، ونجاح `test_anti_flicker_inline_script_in_root_layout`.
- [x] `dir` غير معروض، ويتبع اللغة — الدليل: خلو شاشة المظهر من أي حقل أو زر اتجاه وتأكيد تبعيته للغة حصراً عبر `locale`، ونجاح فحص grep و `test_dir_not_displayed_in_appearance_page`.
- [x] `--brand-h` و`--accent` غير معروضين، و`--brand-h` ما زال متغيّراً حرّاً — الدليل: خلو الواجهة من خيارات الألوان الشخصية وبقاء `--brand-h: 265;` حراً في `globals.css`، ونجاح `test_brand_hue_and_accent_not_in_page_and_free_token`.
- [x] `radius` منزلق حرّ ٤–٢٢ بخطوة ١، و`--rs` يتبعه باشتقاق `max(4, --r - 4)` — الدليل: فحص `input[type="range"]` بالمدى 4–22 والخطوة 1 واشتقاق `--rs` بدقة، ونجاح `test_radius_slider_and_derived_rs`.
- [x] لا حافة مكتوبة رقماً ثابتاً في `components/ui/` ولا في `web/app/**` — الدليل: الاعتماد الصارم على `var(--r)` و `var(--rs)` في الصفحة وكافة مكونات UI، ونجاح `test_no_hardcoded_radii_in_appearance_page` و `test_zero_fixed_border_radius_in_ui_components`.
- [x] «إعادة الضبط» يعمل — الدليل: فحص `resetAppearanceSettings` ومسح المفتاح الموحد والقديمة واستعادة الافتراضيات، ونجاح `test_reset_functionality_clears_keys`.
- [x] `localStorage` معطَّل أو تالف ← الافتراضيات، لا شاشة بيضاء — الدليل: فحص حماية `try...catch` ودالة `sanitizeAppearanceSettings` في `web/lib/appearance.ts`، ونجاح الاختبارات المقابلة.
- [x] صفر نداء شبكة من هذه الشاشة — الدليل: فحص grep على `web/app/settings/appearance/page.tsx` بخروج كود 1 (صفر نتائج مطابقة لـ fetch/api/axios)، ونجاح `test_zero_network_calls_in_appearance_page`.
- [x] النصوص في كتالوج i18n — الدليل: فحص `catalogs/settings.ts` وتوفر كافة المفاتيح الـ 27 باللغتين العربية والإنجليزية، ونجاح `test_appearance_i18n_catalog_keys_complete`.

**تعريف الإنجاز:**
- [x] الاختبارات مكتوبة وناجحة — نجاح 35 اختباراً في حزمة الواجهة والمظهر والرموز والقشرة والخطوط (`tests/test_appearance_settings.py`, `tests/test_shell_navigation.py`, `tests/test_ui_components.py`, `tests/test_theme_tokens.py`, `tests/test_font_tokens.py`).
  - حالات النجاح المغطاة: الأنماط الأربعة، الكثافات الثلاث (18px/14px/10px)، منزلق الزوايا (4-22)، طي الشريط، الترحيل من المفاتيح القديمة، التدويل الكامل ar/en، وإعادة الضبط للافتراضيات.
  - حالات النجاح غير المغطاة: لا شيء.
  - حالات الفشل المغطاة: تلف بيانات التخزين المحلي، انعدام صلاحية التخزين، تجاوز مدى الزوايا، صفر نداء شبكة، وانعدام الحواف الثابتة.
  - حالات الفشل غير المغطاة: لا شيء.
- [x] أوامر العقد — فحص الأنواع `npx --prefix web tsc -p web/tsconfig.json --noEmit` كود 0؛ بناء الإنتاج `npm --prefix web run build` كود 0 لـ 31 صفحة من بينها `/settings/appearance`.
- [x] البوابات المفعّلة — بوابة `i18n` ممتثلة بالكامل؛ بوابة `rtl` ممتثلة بالخصائص المنطقية؛ خلو تام من أنماط `security_scan_patterns` في الكود؛ خلو من الأكواد الميتة ومخرجات التنقيح.
- [x] سياسة الالتزام — التزام واحد نظيف محدث عبر `--amend` بصيغة العقد.
- [x] لا ارتداد — كافة اختبارات الواجهة والرموز والمكونات تعمل بنجاح (35/35).
- [x] توافق الاعتماديات — التوافق التام مع T-048 (الرموز الحرة) و T-051 (القشرة وشريط التنقل).

**الأدلة:**
- `npx --prefix web tsc -p web/tsconfig.json --noEmit` -> كود 0 (خلو تام من أخطاء الأنواع).
- `npm --prefix web run build` -> كود 0 (Next.js 16.2.12 Compiled successfully, 31 static pages, route `/settings/appearance` generated).
- `uv run pytest tests/test_appearance_settings.py tests/test_shell_navigation.py tests/test_ui_components.py tests/test_theme_tokens.py tests/test_font_tokens.py -v` -> 35 passed in 1.16s.
- `git diff 502791b..HEAD -- web/ tests/ | grep -iE "api_key|secret|password|token=|DATABASE_URL|CLERK_SECRET_KEY"` -> كود 1 (صفر مطابقة، خلو تام من الأسرار).
- `grep -iE "fetch|lib/api|axios|XMLHttpRequest" web/app/settings/appearance/page.tsx` -> كود 1 (صفر نداء شبكة).

**المشاكل المكتشفة:**
- لا توجد أي ملاحظات حاجبة أو أخطاء.
- ملاحظة غير حاجبة: بنود التحقق البشري العشرة (`sections.human_verification`) متروكة للمستخدم لاختبار السلوك البصري الحي في المتصفح، واعتماد المراجعة الآلية لا يغني عنها.

**الالتزام:** عُدِّل التزام المنفّذ عبر `git commit --amend` لضم المراجعة ونسب الإنجاز الكاملة (AC: 11/11 · DoD: 4/4).

**التوصية:**
التذكرة معتمدة ومستوفاة وجاهزة تماماً للمزامنة والمراجعة البشرية النهائية دون أي موانع.

