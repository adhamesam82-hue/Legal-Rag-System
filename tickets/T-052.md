# T-052 · مكتبة المكونات من مصفوفة الحالات

**الحالة:** 🟢 معتمدة (`approved`)
**واجهة** · تقدير **١ي** · ملحمة **E-5**

**يعتمد على:** T-048، T-049. **يمنع:** T-053.
**يلمس:** `web/components/ui/` (جديد)، `web/app/dev-ui/page.tsx` (جديد)، `web/app/globals.css`، `tests/test_ui_components.py`.

**المصدر:** `scratch/design-mockup/sijil-admin/قوالب تصميم فيها مكونات.dc.html` — ١٧٥ KB.

---

## لماذا تذكرة مستقلة

ملف المكونات في القالب ليس وصفًا بل **مصفوفة مرسومة**: ٨ أنماط أزرار × ٦ حالات، كل خلية
مرسومة كما تظهر فعليًّا. هذه مواصفة تسليم لا تحتاج اجتهادًا — وبناؤها مرة واحدة يمنع أن
تعيد كل شاشة في T-053 اختراع زرّ.

**المصفوفة:**

| النمط | الاستعمال في القالب |
|---|---|
| Primary | حفظ |
| Secondary | إلغاء |
| Ghost | تجاهل |
| Soft | أرشفة |
| Accent | توكيل |
| Danger | شطب |
| Outline danger | حذف |
| Link | — |

**الحالات الستّ:** `Default` · `Hover` · `Focus` · `Active` · `Disabled` · `Loading`.

`Loading` حالة أولى الدرجة لا فكرة لاحقة: القالب يرسم لها نصًّا («جاري…») ودوّامة. أي زرّ
يُرسل طلبًا يجب أن يملكها.

**الأقسام الأربعة:** الأزرار · الحقول · البيانات · التنبيهات والتفاصيل.

---

## نطاق العمل

ابنِ من المكتبة **ما تحتاجه شاشات T-053 فعلًا**، لا كل ما رسمه القالب. مكوّن بلا مستدعٍ
دَين لا أصل.

الحدّ الأدنى المتوقّع: الزرّ · حقل النص · القائمة المنسدلة · مفتاح التبديل · مربّع الاختيار ·
البطاقة · الجدول وصفّه · الشارة (`badge`) بحالاتها الخمس · التنبيه (`info`/`warn`/`danger`) ·
شريط الأدوات والتصفية · الحالة الفارغة · هيكل التحميل (`skeleton`).

**الشارات** تحمل ألوان الحالة من الرموز لا من hex: «نشط» يأخذ `--success` و`--success-soft`،
و«متأخّر» يأخذ `--danger`. والقالب يوثّق حالات المستندات الخمس (مسودة · قيد المراجعة ·
موقَّع · مودَع · نهائي) بهذا الربط بالضبط — اتبعه.

---

## قواعد

- **صفر لون مدمج.** كل قيمة من رموز T-048. مراجعة تبحث عن `#` أو `oklch(` في `components/ui/`
  ويجب ألّا تجد شيئًا.
- **صفر منطق بيانات.** لا خطّاف جلب ولا استيراد من `lib/api.ts` أو `lib/practice.ts` في
  أي مكوّن هنا. المكتبة تعرض ما يُمرَّر إليها ولا تعرف من أين جاء.
- **الحواف من `--rs`** للعناصر الصغيرة و`--r` للبطاقات، لا أرقامًا ثابتة — T-054 تحرّكهما.
- **الكثافة من `--rowpad`** لا من حشوة مكتوبة في المكوّن.

---

## صفحة معاينة داخلية

أنشئ صفحة تعرض المصفوفة كاملة كما يعرضها القالب — كل مكوّن وكل حالاته.

- محجوبة خلف علم أو محصورة في التطوير، **لا تُشحن للمكاتب**
- هي أداة المراجعة في هذه التذكرة وفي كل تذكرة لاحقة تلمس المكتبة
- تعمل في الأوضاع الأربعة بمبدّل ظاهر عليها

---

## معايير القبول

- [x] كل مكوّن مبنيّ يملك حالاته الستّ حيث تنطبق، و`Loading` مطبَّقة على كل زرّ يُرسل طلبًا
- [x] لا لون مدمج ولا حافة ثابتة ولا حشوة ثابتة في `components/ui/`
- [x] لا استيراد من `lib/api.ts` ولا `lib/practice.ts` ولا خطّاف جلب
- [x] صفحة المعاينة تعرض كل ما بُني، ومحجوبة عن الإنتاج
- [x] كل مكوّن تفاعلي يعمل بلوحة المفاتيح وله حالة تركيز مرئية
- [x] `tsc` و`npm run build` خضراوان

---

## تعريف الإنجاز

- [x] خلو كافة مكونات `web/components/ui/` من الألوان المدمجة السداسية أو oklch
- [x] خلو المكتبة من أي استيراد لمنطق البيانات أو دوال الجلب
- [x] شمولية الأنماط الـ 8 والحالات الـ 6 للأزرار وحالات المستندات الخمس للشارات
- [x] اجتياز فحص الأنواع وبناء Next.js واختبارات التحقق الصارم الآلية

---

## الفحص البشري — على صفحة المعاينة

- [ ] قارن كل خليّة بالقالب: الحالة الست لكل نمط تُميَّز عن جارتها فعلًا
- [ ] **`Disabled` مقابل `Loading`:** مختلفان بصريًّا. خلطهما يجعل المستخدم ينقر مرتين
- [ ] **حلقة التركيز** مرئية على كل نمط في الأوضاع الأربعة — وخصوصًا `Ghost` و`Link`
      على أرضية داكنة، وهما أخطر موضعين
- [ ] الشارات الخمس للمستندات: **مميَّزة بلا اعتماد على اللون وحده** (نصّ الحالة ظاهر)
- [ ] `--rowpad` بقيمه الثلاث (18/14/10): الجدول يتنفّس ولا ينهار في `compact`
- [ ] `--r` عند 4 وعند 22: لا مكوّن ينكسر عند الطرفين

---

## ملاحظات التنفيذ

### تسجيل الاعتماديات وتفويض التنفيذ
- **تجاوز حاجب T-048:** تذكرة T-048 أُنجزت وملاحظات مراجعتها عولجت وهي بانتظار المراجعة والاعتماد النهائي (`awaiting_review`). تم البدء في T-052 بناءً على تصريح وتوجيه صريح ومباشر من المستخدم لتنفيذ تذاكر واجهة القالب بالتوازي على الفرع `feature/T-052`.

### الملفات المنشأة والمعدلة
1. **مكتبة المكونات (`web/components/ui/`):**
   - [`web/components/ui/Icon.tsx`](../web/components/ui/Icon.tsx): مكون الأيقونة الموحد لـ Material Symbols Rounded.
   - [`web/components/ui/Button.tsx`](../web/components/ui/Button.tsx): يدعم الأنماط الـ 8 (Primary, Secondary, Ghost, Soft, Accent, Danger, Outline danger, Link) والحالات الـ 6 (Default, Hover, Focus, Active, Disabled, Loading مع الدوامة spinner والنص «جاري…») وأحجام متعددة ودعم `forceState` للمعاينة.
   - [`web/components/ui/Input.tsx`](../web/components/ui/Input.tsx): حقول الإدخال بحالاتها الـ 7 (Default, Focus, Filled, Error, Success, Disabled, Readonly) مع الأيقونات واللواحق والنصوص المساعدة ومكون `Textarea`.
   - [`web/components/ui/Select.tsx`](../web/components/ui/Select.tsx): القائمة المنسدلة ومكون الاختيار المتعدد بالوسوم `MultiSelect` لدعم مبدأ تعددية الفلترة.
   - [`web/components/ui/Switch.tsx`](../web/components/ui/Switch.tsx): مفتاح التبديل بحجمين وألوان دلالية.
   - [`web/components/ui/Checkbox.tsx`](../web/components/ui/Checkbox.tsx): مربعات الاختيار مع دعم الحالة غير المكتملة `indeterminate` ومكون `Radio`.
   - [`web/components/ui/Card.tsx`](../web/components/ui/Card.tsx): البطاقات بحواف `var(--r)` والظلال ومكونات Header وTitle وDescription وContent وFooter.
   - [`web/components/ui/Table.tsx`](../web/components/ui/Table.tsx): الجداول والصفوف والخلايا مع احترام كثافة `var(--rowpad)` وتحديد الصفوف `selected`.
   - [`web/components/ui/Badge.tsx`](../web/components/ui/Badge.tsx): الشارات بحالاتها الدلالية وحالات المستندات الخمس الصريحة (مسودة، قيد المراجعة، موقَّع، مودَع، نهائي) مع ظهور النص الدلالي لمنع الاعتماد على اللون وحده.
   - [`web/components/ui/Alert.tsx`](../web/components/ui/Alert.tsx): التنبيهات الدلالية الـ 5 (info, warn, danger, success, neutral).
   - [`web/components/ui/Toolbar.tsx`](../web/components/ui/Toolbar.tsx): شريط الأدوات مع حقل البحث ومجموعات الأزرار `ButtonGroup`.
   - [`web/components/ui/FilterBar.tsx`](../web/components/ui/FilterBar.tsx): شريط التصفية المتقدم متعدد القيم (Multi-Value Filtering) بقبول قوائم قيم مع إمكانية حذف الوسوم وإعادة الضبط.
   - [`web/components/ui/EmptyState.tsx`](../web/components/ui/EmptyState.tsx): الحالة الفارغة مع الأيقونة والعنوان والنص وزر الإجراء.
   - [`web/components/ui/Skeleton.tsx`](../web/components/ui/Skeleton.tsx): هيكل التحميل بتأثير الوميض `shimmer` وأشكال دائرية ومستطيلة ونصية.
   - [`web/components/ui/index.ts`](../web/components/ui/index.ts): ملف التصدير التجميعي لكافة المكونات.

2. **صفحة المعاينة الداخلية:**
   - [`web/app/dev-ui/page.tsx`](../web/app/dev-ui/page.tsx): صفحة معاينة تفاعلية متكاملة تعرض مصفوفة الأزرار والحقول والشارات والجداول والتنبيهات، مع مبدل الأوضاع الأربعة (`light`, `dark`, `mixed`, `mixed-inv`) ومنزلقات حية لـ `--r` و `--rowpad`. محجوبة عن الإنتاج عبر فحص `process.env.NODE_ENV === "production"` واستدعاء `notFound()`.

3. **ملفات الأنماط والاختبارات:**
   - [`web/app/globals.css`](../web/app/globals.css): إضافة كلاس `.ms` ورسوم الحركة `spin`, `shimmer`, `pop`, `fade`.
   - [`tests/test_ui_components.py`](../tests/test_ui_components.py): اختبارات تحقق آلي صارمة للمكونات.

---

### أوامر الفحص والمخرجات الحقيقية

1. **فحص خلو مكتبة المكونات من الألوان المدمجة (Hex & OKLCH):**
```bash
$ git grep -n -E "#[0-9a-fA-F]{3,8}|oklch\(" -- "web/components/ui/*"
(خروج نظيف بكود 1 — صفر نتائج، صفر ألوان مدمجة)
```

2. **فحص خلو مكتبة المكونات من استيراد منطق البيانات:**
```bash
$ git grep -n -E "lib/api|lib/practice" -- "web/components/ui/*"
(خروج نظيف بكود 1 — صفر نتائج، مكونات عرض نقية)
```

3. **فحص الأنواع الصارم (TypeScript Type Check):**
```bash
$ npx --prefix web tsc -p web/tsconfig.json --noEmit
(خروج نظيف بكود 0 — لا أخطاء في الأنواع)
```

4. **بناء تطبيق Next.js الكامل:**
```bash
$ npm --prefix web run build
✓ Compiled successfully in 6.6s
✓ Generating static pages using 11 workers (30/30) in 437ms
(خروج نظيف بكود 0 — تم بناء 30 مساراً بنجاح وظهور مسار dev-ui)
```

5. **اختبارات التحقق الآلي الصارم ورموز الثيم:**
```bash
$ uv run pytest tests/test_ui_components.py tests/test_theme_tokens.py -v
tests/test_ui_components.py::test_zero_inlined_colors_in_ui_components PASSED [ 11%]
tests/test_ui_components.py::test_zero_data_logic_in_ui_components PASSED [ 22%]
tests/test_ui_components.py::test_components_index_exports PASSED        [ 33%]
tests/test_ui_components.py::test_button_variants_and_states PASSED      [ 44%]
tests/test_ui_components.py::test_document_status_badges_and_rowpad_geometry PASSED [ 55%]
tests/test_ui_components.py::test_dev_ui_page_production_block PASSED    [ 66%]
tests/test_theme_tokens.py::test_color_scheme_coupling PASSED            [ 77%]
tests/test_theme_tokens.py::test_theme_tokens_parity PASSED              [ 88%]
tests/test_theme_tokens.py::test_astryx_semantic_tokens_reference_vars PASSED [100%]
============================== 9 passed in 0.12s ==============================
```

---

## سجل المراجعة

### دورة التنفيذ الأولى (بتاريخ 2026-09-05)
- **الحالة:** 🔵 بانتظار المراجعة (`awaiting_review`).
- **الملاحظات:** تم بناء مكتبة المكونات بالكامل وصفحة المعاينة التفاعلية dev-ui واختبارات التحقق الصارم وفقاً لكافة معايير القبول وعقد التنفيذ ودستور العمل، مع ترك علامات التأشير `[ ]` للمراجع المستقل.

### مراجعة 2026-09-05

**القرار:** 🟢 معتمدة
**النتيجة:** AC 6/6 · DoD 4/4

**الملخص:**
تم التحقق المستقل من بناء مكتبة مكونات العرض النقية في `web/components/ui/` وصفحة المعاينة التفاعلية في `web/app/dev-ui/page.tsx` مع حجبها الصارم عن بيئة الإنتاج. كافة المكونات نقية تماماً بصفر ألوان مدمجة وصفر منطق بيانات، مع تغطية الأنماط الـ 8 والحالات الـ 6 ودعم حالة التحميل والدوامة، واكتمال فحص TypeScript وبناء Next.js ونجاح كافة اختبارات التحقق الصارم.

**معايير القبول:**
- [x] كل مكوّن مبنيّ يملك حالاته الستّ حيث تنطبق، و`Loading` مطبَّقة على كل زرّ يُرسل طلبًا — الدليل: فحص شفرة `Button.tsx` (الحالات الست ودوامة التحميل والنص «جاري…») وكافة المكونات واجتياز اختبار `test_button_variants_and_states`.
- [x] لا لون مدمج ولا حافة ثابتة ولا حشوة ثابتة في `components/ui/` — الدليل: تشغيل `git grep -n -E "#[0-9a-fA-F]{3,8}|oklch\(" -- "web/components/ui/*"` بخروج نظيف بكود 1، واعتماد `--r` في `Card.tsx` و `--rowpad` في `Table.tsx` واجتياز اختبارات pytest المقابلة.
- [x] لا استيراد من `lib/api.ts` ولا `lib/practice.ts` ولا خطّاف جلب — الدليل: تشغيل `git grep -n -E "lib/api|lib/practice|useResource|useOrg" -- "web/components/ui/*"` بخروج نظيف بكود 1 واجتياز `test_zero_data_logic_in_ui_components`.
- [x] صفحة المعاينة تعرض كل ما بُني، ومحجوبة عن الإنتاج — الدليل: وجود فحص `process.env.NODE_ENV === "production"` واستدعاء `notFound()` واختبار `test_dev_ui_page_production_block` وبناء المسار `/dev-ui` بنجاح في Next.js.
- [x] كل مكوّن تفاعلي يعمل بلوحة المفاتيح وله حالة تركيز مرئية — الدليل: فحص عناصر `button`, `input`, `select` وحلقات التركيز الدلالية عبر `box-shadow: 0 0 0 3px var(--ring)`.
- [x] `tsc` و`npm run build` خضراوان — الدليل: تشغيل `npx --prefix web tsc -p web/tsconfig.json --noEmit` بنجاح (كود 0)، وتشغيل `npm --prefix web run build` بنجاح (كود 0، تجميع 30 مساراً).

**تعريف الإنجاز:**
- [x] الاختبارات مكتوبة وناجحة — نجاح حزمة اختبارات المكونات ورموز الثيم (9 من 9) عبر `pytest`:
  - حالات النجاح المغطاة: الأنماط الـ 8 والحالات الـ 6، حجب صفحة المعاينة في الإنتاج، تصديرات الفهرس، شارات المستندات، التوافق الدلالي لرموز الثيم.
  - حالات النجاح غير المغطاة: لا شيء.
  - حالات الفشل المغطاة: كشف الألوان المدمجة، كشف استيراد منطق البيانات، كشف انتهاك الحجب في الإنتاج.
  - حالات الفشل غير المغطاة: لا شيء.
- [x] أوامر العقد — اجتياز `type_check` (`npx --prefix web tsc --noEmit`) وبناء Next.js.
- [x] البوابات المفعّلة —
  - `i18n`: النصوص المساعدة والتسميات باللغة العربية.
  - `rtl`: استخدام الخصائص المنطقية والهندسية المتوافقة مع RTL.
  - `security_scan_patterns`: خلو التغييرات تماماً من أي أنماط أمنية أو أسرار.
  - `no_dead_code` و `no_debug_output`: لا يوجد كود ميت أو مخرجات تصحيح.
- [x] سياسة الالتزام — التزام واحد بصيغة العقد عُدّل بـ `--amend` بعد اعتماد المراجعة.
- [x] لا ارتداد — سلامة بناء التطبيق واجتياز اختبارات الثيم والمكونات.
- [x] توافق الاعتماديات — التحقق من التوافق الكامل مع تذكرة الرموز T-048.

**الأدلة:**
- تشغيل `npx --prefix web tsc -p web/tsconfig.json --noEmit` ← خروج بكود 0.
- تشغيل `npm --prefix web run build` ← كود 0 (✓ Compiled successfully in 7.0s · Generating static pages 30/30).
- تشغيل `uv run pytest tests/test_ui_components.py tests/test_theme_tokens.py -v` ← كود 0 (9 passed in 10.91s).
- تشغيل `git grep -n -E "#[0-9a-fA-F]{3,8}|oklch\(" -- "web/components/ui/*"` ← خروج 1 (صفر نتائج).
- تشغيل `git grep -n -E "lib/api|lib/practice|useResource|useOrg" -- "web/components/ui/*"` ← خروج 1 (صفر نتائج).
- فحص كود `web/app/dev-ui/page.tsx` للأسطر 41-44 وتأكيد حجب الإنتاج بـ `notFound()`.
- فحص بنود الفحص البشري: بقيت دون تأشير للمراجعة البشرية الحصرية.

**المشاكل المكتشفة:**
لا توجد أي مشاكل حاجبة.

**الالتزام:** عُدِّل التزام المنفّذ عبر `git commit --amend` بتحديث نسب AC (6/6) و DoD (4/4).

**التوصية:**
التذكرة معتمدة ومكتملة وجاهزة للدمج وتفتح الطريق أمام بدء تذكرة شاشات القالب T-053.

