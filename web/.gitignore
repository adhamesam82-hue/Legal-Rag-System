node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
.env*.local

# Local screenshot/smoke scratch scripts
shot*.mjs
shots-all.mjs
final.mjs
smoke.mjs
.tmp-*.mjs
verify-live.mjs

# Playwright QA scratch scripts (session-local, not part of the app)
.qa-*.mjs

# Fonts the brand-mark generator downloads (scripts/brand/build-marks.py).
# Upstream, pinned in the script, and 650KB — fetched on demand, not committed.
scripts/brand/.cache/
