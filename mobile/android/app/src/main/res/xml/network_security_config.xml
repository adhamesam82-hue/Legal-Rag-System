<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is permitted to the loopback host only.

  Android blocks cleartext by default from API 28, which is correct: the
  deployed API is HTTPS and must stay that way. But the local backend runs as
  plain http://10.0.2.2:8000 (the emulator's route to the host machine), and
  without this exception it fails with a bare "cleartext not permitted" that
  reads like the server is down.

  Scoped to the two loopback addresses rather than opened globally, so no
  real host can ever be reached without TLS.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
